import Roles from 'src/security/roles';

const studentEnumerators = {
  status: ['active', 'pre-registered'],
  bloodType: ['O', 'A', 'B', 'AB'],
  sex: ['male', 'female'],
};

export default studentEnumerators;
