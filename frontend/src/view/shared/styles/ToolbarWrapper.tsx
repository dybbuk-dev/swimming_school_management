import { styled } from '@mui/material/styles';

const ToolbarWrapper = styled('div')({
  display: 'flex',
  alignItems: 'center',
  flexWrap: 'wrap',
  gap: '6.4px',
});

export default ToolbarWrapper;
